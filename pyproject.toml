[project]
name = "snapdragon-npu-audio-enhancer"
version = "0.1.0"
description = "Reference DSP and NPU orchestration for Snapdragon X audio enhancement."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
